


```
openssl pkcs12 -in elastic-certificates.p12 -clcerts -nokeys -out elastic-certificates.pem  //pem格式
openssl pkcs12 -in elastic-certificates.p12 -clcerts -nokeys -out elastic-certificates.crt  //crt格式
```
